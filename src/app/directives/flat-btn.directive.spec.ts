import { FlatBtnDirective } from './flat-btn.directive';

describe('FlatBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new FlatBtnDirective();
    expect(directive).toBeTruthy();
  });
});
