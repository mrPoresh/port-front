import { FillBtnDirective } from './fill-btn.directive';

describe('FillBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new FillBtnDirective();
    expect(directive).toBeTruthy();
  });
});
