import { ToggBtnDirective } from './togg-btn.directive';

describe('ToggBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggBtnDirective();
    expect(directive).toBeTruthy();
  });
});
